apiVersion: apps/v1
kind: Deployment
metadata:
  name: vink-ui
  namespace: {{ .Values.namespace }}
  labels:
    app: vink-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vink-ui
  template:
    metadata:
      labels:
        app: vink-ui
    spec:
      containers:
      - name: vink-ui
        image: docker.io/hejianmin/vink-ui@sha256:91062b3f51f9a1614df1f2145df5a81c9729ef457602830aece1aa87a0a819d5
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        volumeMounts:
        - readOnly: true
          mountPath: /etc/nginx/conf.d
          name: nginx-conf
      volumes:
        - name: nginx-conf
          configMap:
            name: vink-nginx
