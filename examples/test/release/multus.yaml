apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: multus-cni
  namespace: flux-system
spec:
  dependsOn:
    - name: kube-ovn
      namespace: flux-system
  interval: 10m
  timeout: 10m
  chart:
    spec:
      chart: ./examples/multus/multus
      sourceRef:
        kind: GitRepository
        name: vink
        namespace: flux-system
  targetNamespace: kube-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3

# apiVersion: kustomize.toolkit.fluxcd.io/v1
# kind: Kustomization
# metadata:
#   name: multus-cni
#   namespace: flux-system
# spec:
#   # dependsOn:
#   #   - name: kube-ovn
#   #     namespace: flux-system
#   interval: 5m
#   timeout: 3m
#   retryInterval: 1m
#   path: ./manifests/multus
#   prune: true
#   sourceRef:
#     kind: GitRepository
#     name: vink
#     namespace: flux-system
#   targetNamespace: kube-system
#   wait: true
